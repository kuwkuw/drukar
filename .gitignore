node_modules/
dist/
coverage/
*.tsbuildinfo

# runtime data (job state + mesh artifacts); DRUKAR_DATA_DIR defaults to ./data,
# relative to whichever app's cwd runs it (e.g. apps/api/data in dev)
data/

# environment
.env
.env.local

# editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/

# local Claude Code permissions
.claude/settings.local.json
